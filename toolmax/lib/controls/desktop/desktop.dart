import 'package:flutter/material.dart';

class DesktopControls extends StatelessWidget {
  const DesktopControls({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('DesktopControls');
  }
}
